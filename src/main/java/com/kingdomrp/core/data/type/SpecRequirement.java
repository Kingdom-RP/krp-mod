package com.kingdomrp.core.data.type;

/**
 * Требование уровня КОНКРЕТНОЙ специализации (а не пути) для крафта/ношения.
 * Заменил {@code TierRequirement(Path, level)} — крафт/ношение профессионально
 * привязаны к спеку (Кузнец кует, Воин носит броню), а не к пути целиком.
 * <p>
 * Для крафта используется список таких требований (все должны выполняться) —
 * это позволяет гейтить предмет сразу двумя спеками (напр. золотое яблоко =
 * Алхимик 3 + Зачарователь 3). См. {@link ItemCraftTierMap}.
 */
public record SpecRequirement(Spec spec, int level) {}
